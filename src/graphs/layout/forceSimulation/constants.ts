import { Vector } from "../../vector/Vector";

export const DEFAULT_CENTER = new Vector(0.5, 0.5);
export const DEFAULT_EDGE_MIN_DISTANCE = 0.001;
export const DEFAULT_MAX_DELTA_MS = 50;
export const BOUNDS_BOUNCE_FACTOR = -0.35;
export const ZERO = new Vector(0, 0);
