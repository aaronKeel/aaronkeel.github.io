Graph.ts defines the data structure to represent a graph abstractly.
The main componets of a graph are its vertices and edges which are represented by the classes within Vertex.ts and Edge.ts.
The Vector class is a helper class to define some geometric aspects of graphs and provide some geometric computation with the use of vectors.
generators.ts contains algorithms for generating basic graph structures.